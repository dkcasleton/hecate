apply plugin: 'groovy'
apply plugin: 'eclipse'

group = 'brewhot.weasel'

repositories {
	mavenCentral()
}

dependencies {
	compile localGroovy()
	
	compile 'asm:asm:3.2'
	compile 'asm:asm-commons:3.2'
	
	testCompile 'junit:junit:4.10'
}

task wrapper(type: Wrapper) {
	gradleVersion = '1.6'
}